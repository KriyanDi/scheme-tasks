;оптимизацията идва от 7 ред, защото когато се оцени с #t на 7 ред, осми ред няма да се извика

(define (exists? p? a b)
  (if (< a b)
      (if (p? a)
          #t
          (or (p? a)
              (exists? (+ a 1) b p?)))
      #f))